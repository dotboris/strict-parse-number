/**
 * Parse number from the given string. Supports parsing integers and decimal
 * point numbers.
 *
 * @param {string} string - text to parse the number out of.
 * @returns {number} Parsed number value if valid, NaN otherwise
 */
export function strictParseNumber (string) {

}
